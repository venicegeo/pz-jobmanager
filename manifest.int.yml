applications:
- name: pz-jobmanager
  host: pz-jobmanager
  path: pz-jobmanager.jar
  buildpack: java_buildpack_offline
  memory: 512M
  instances: 1
  timeout: 80
  services:
    - pz-kafka
    - pz-mongodb
  env:
    SPRING_PROFILES_ACTIVE: cloud
